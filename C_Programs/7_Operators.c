#include<stdio.h>
#include<conio.h>
void main(){
    clrscr();
    // Total 5 Types of Operator.
    printf("1. Arithmatic Operator: +, -, *, /, modulo ")
    printf("2. Arithmatic Assignment Operator: +=, -=, *=, /=, modulo= ")
    printf("3. Relational Operator: < , <= , > , >= , == , != ")
    printf("4. Logical Operator: &&, ||, !(&&), !(||) ")
    printf("5. Bitwise Operator: &, |, ~, ^, <<, >> ")
    getch();
}