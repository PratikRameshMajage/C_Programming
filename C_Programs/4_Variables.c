#include<stdio.h>
#include<conio.h>
void main(){
    clrscr();
    // Variable : Pointing Address of memory storage name.
    // Data Type Variable Name = Value;
    // & -> use to pointout the Address of memory storage name.
    // Declare the Variable.
    int a;
    printf("value is: %d",a);
    // Defining the Variable.
    int b = 2;
    printf("value is: %d",b);
    getch();
}